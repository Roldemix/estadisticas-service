apiVersion: v1
kind: Service
metadata:
  name: estadisticas
  namespace: casino
spec:
  selector:
    app: estadisticas
  ports:
    - protocol: TCP
      port: 8006
      targetPort: 8006
  type: ClusterIP